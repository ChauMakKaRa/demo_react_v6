import clsx from 'clsx';
import styles from './Project.module.scss';
function ProjectPageAdmin() {
    return (
        <div className={clsx(styles.wrapper)}>
            <div>Project page admin</div>
        </div>
    );
}

export default ProjectPageAdmin;
